<h1><%= @recipes.name %></h1>

<h3>Ingredients</h3>

<% if @recipes.ingredient.any? %>
<p>You will need the following ingredients:</p>
  <% @recipes.ingredient.each do |t| %>
  <ul>
    <li><%= t %></li>
  </ul>
  <% end %>
<% else %>
  <p>You forgot to add the ingredients for this recipe!</p>
<% end %>

<h3>Instructions</h3>

<% if @recipes.instruction.empty? %>
  <p>You forgot to include the instructions! You will probably need to edit!</p>
<% else %>
  <p>Follow these instructions carefully!</p>
  <%= @recipes.instruction %>
<% end %>

<h4>Update Recipe</h4>
<p>Oops! If you'd like to change a recipe, you can edit <a href="/recipe/<%= @recipes.id %>/edit">here.</a></p>

<h4>Delete</h4>
<form action="/recipes/<%= @recipes.id %>/delete" method="post">
  <p>If you're sure you don't like this recipe anymore, go ahead and click the button.</p>
  <input type="hidden" name="_method" value="delete">
  <button type="submit" class="btn">Delete!</button>
</form>
